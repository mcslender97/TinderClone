<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mcslender.tinder">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.mcslender.tinder.MainActivity" />
        <activity android:name="com.mcslender.tinder.ChooseLoginRegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.mcslender.tinder.RegistrationActivity" />
        <activity android:name="com.mcslender.tinder.LoginActivity" />
        <activity android:name="com.mcslender.tinder.SettingsActivity" />
        <activity android:name="com.mcslender.tinder.Matches.MatchesActivity" />
        <activity android:name="com.mcslender.tinder.Chat.ChatActivity"></activity>
    </application>

</manifest>